/*
use crate::Iter;
use crate::lexer::{ Token, TokenIter, Number };

enum Ast {
    Expr(Expr),
    Patt(Patt),
}
enum Patt {
    CatchAll
}
enum Expr {
    ConstNum(Number),
    Unit,
}

fn build (tokens:TokenIter) -> Ast {
    Ast::Expr(Expr::Unit)
}
*/
